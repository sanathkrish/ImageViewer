<Window
    x:Class="ImageViewer.Controls.InitialWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:Controls="using:ImageViewer.Controls.UI"
    xmlns:NavControl="using:ImageViewer.Controls"
    Title="Duplicate Cleaner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <!-- Top Header Bar -->
        <Grid Grid.Column="0" Grid.Row="0" Padding="20,16" Background="#151B2D">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Logo + Title -->
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Border Width="32" Height="32" CornerRadius="6" Background="#2563EB">
                    <FontIcon Glyph="&#xE8A5;" Foreground="White"/>
                </Border>
                <StackPanel>
                    <TextBlock Text="Duplicate Cleaner" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock Text="Find duplicates, similar &amp; blurry images" FontSize="12" Foreground="#8B93A7"/>
                </StackPanel>
            </StackPanel>

            <!-- Scan Status -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE930;" Foreground="#10B981"/>
                <StackPanel>
                    <TextBlock Text="Scan completed" Foreground="White" FontWeight="SemiBold"/>
                    <TextBlock Text="Scan time: 00:15:42" FontSize="12" Foreground="#8B93A7"/>
                </StackPanel>
            </StackPanel>

            <!-- Actions -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                <Button Style="{StaticResource AccentButtonStyle}" Command="{x:Bind vm.StartNewScanCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="New Scan"/>
                    </StackPanel>
                </Button>
                <Button Width="40" Height="36">
                    <FontIcon Glyph="&#xE712;"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Main 3-Column Layout -->
        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" />
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Nav -->
            <ScrollViewer  Grid.Column="0">
                <StackPanel Spacing="4">
                    <NavControl:NavigationControl />
                </StackPanel>
            </ScrollViewer>

            <!-- Center Content -->
            <Frame  Grid.Column="1" x:Name="main_window"></Frame>
           
        </Grid>
    </Grid>
</Window>