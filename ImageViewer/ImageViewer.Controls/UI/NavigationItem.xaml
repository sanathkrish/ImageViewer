<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ImageViewer.Controls.UI.NavigationItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ImageViewer.Controls.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Converters="using:ImageViewer.Controls.ValueConverters"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Converters:BoolToVisibilityValueConverter x:Key="BoolToVisibilityValueConverter"></Converters:BoolToVisibilityValueConverter>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal" Spacing="12" Margin="12" Visibility="{Binding UseStandard, Converter={StaticResource BoolToVisibilityValueConverter}}">
            <!--<FontIcon Glyph="{Binding Icon}" FontSize="16" Foreground="White"/>-->
            <TextBlock Text="{Binding Name}" FontSize="14" Foreground="White"/>
        </StackPanel>
        
        <StackPanel x:Name="ContextMenu" Visibility="{Binding UseCustom, Converter={StaticResource BoolToVisibilityValueConverter}}">
            <TextBlock Text="{Binding Name}" FontSize="14" Foreground="White"/>
        </StackPanel>

    </Grid>
</UserControl>
